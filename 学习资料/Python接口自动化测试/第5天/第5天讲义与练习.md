# Python 接口自动化测试 Day 5 讲义 + 配套练习

## 1. 使用说明

Day 5 的重点不是多写几个测试，而是整理结构。

今天建议你用“边拆边跑”的方式学：

1. 先看 Day 5 教材里的完整示例
2. 再自己建目录
3. 每迁移一个模块就跑一次
4. 不要求一步到位，但要求每一步都能跑

配套教材：

[第5天教材.md](./第5天教材.md)

继续使用的接口文档：

[/Users/Apple/Documents/Codes/PythonTestingProjects/练习项目/本地接口练习服务/接口文档.md](/Users/Apple/Documents/Codes/PythonTestingProjects/练习项目/本地接口练习服务/接口文档.md)

---

## 2. 今日目标

今天你要掌握：

- 目录为什么要分层
- 公共请求方法怎么抽
- 配置和数据怎么分开放
- 测试文件怎么只保留业务意图

---

## 3. 今日准备

开始前确认：

1. 本地接口服务能启动
2. `pytest` 已可用
3. 你已经有 Day 4 的登录测试和商品链路测试

### 3.1 今日建议准备命令

```bash
cd /Users/Apple/Documents/Codes/PythonTestingProjects/练习项目/本地接口练习服务
source .venv/bin/activate
pytest --version
python app/app.py
```

再开一个终端用于跑测试。

---

## 4. 讲义模块一：先建目录

今天先把目录建出来，再迁移代码。

推荐结构：

```text
api_auto_test/
├── common/
├── config/
├── data/
├── testcases/
├── conftest.py
├── pytest.ini
└── requirements.txt
```

### 你要理解

- `common` 放公共方法
- `config` 放配置
- `data` 放测试数据
- `testcases` 放测试文件

### 随堂检查

1. 为什么不建议所有东西都继续堆在根目录？

### 参考答案

1. 因为测试一多，配置、数据、公共逻辑和测试文件会混在一起，后面很难维护。

---

## 5. 讲义模块二：先迁配置

先建：

```text
config/settings.py
```

内容：

```python
BASE_URL = "http://127.0.0.1:5001"
USERNAME = "alice"
PASSWORD = "123456"
TIMEOUT = 5
```

### 你要理解

- 先把 `BASE_URL`、账号密码从测试文件里收走

---

## 6. 讲义模块三：再迁公共请求

新建：

```text
common/request_handler.py
```

内容：

```python
import requests

from config.settings import TIMEOUT


def send_request(method, url, **kwargs):
    kwargs.setdefault("timeout", TIMEOUT)
    return requests.request(method=method, url=url, **kwargs)
```

### 你要理解

- 后面测试不再直接到处写 `requests.get/post/delete`
- 先统一走一个入口

### 随堂检查

1. 为什么这里用 `kwargs.setdefault("timeout", TIMEOUT)`？

### 参考答案

1. 因为这样默认有超时配置，但需要时也可以单独覆盖。

---

## 7. 讲义模块四：再迁公共断言

新建：

```text
common/assert_util.py
```

内容：

```python
def assert_success_response(resp, body, status_code=200):
    assert resp.status_code == status_code
    assert body["code"] == 0
    assert body["message"] == "success"
```

### 你要理解

- 通用断言抽出来后，测试文件会更干净

---

## 8. 讲义模块五：整理 conftest.py

新建或迁移为：

```python
import pytest

from common.request_handler import send_request
from config.settings import BASE_URL, PASSWORD, USERNAME


@pytest.fixture
def reset_data():
    resp = send_request("POST", f"{BASE_URL}/api/v1/debug/reset")
    body = resp.json()

    assert resp.status_code == 200
    assert body["code"] == 0


@pytest.fixture
def login_token(reset_data):
    resp = send_request(
        "POST",
        f"{BASE_URL}/api/v1/login",
        json={"username": USERNAME, "password": PASSWORD},
    )
    body = resp.json()

    assert resp.status_code == 200
    assert body["code"] == 0

    return body["data"]["token"]


@pytest.fixture
def auth_headers(login_token):
    return {"Authorization": f"Bearer {login_token}"}
```

### 你要理解

- Day 5 不是删掉 fixture，而是让 fixture 和公共请求一起工作

---

## 9. 讲义模块六：迁移登录测试

新建：

```text
testcases/test_login.py
```

内容：

```python
import json
from pathlib import Path

import pytest

from common.request_handler import send_request
from config.settings import BASE_URL


DATA_FILE = Path(__file__).resolve().parent.parent / "data" / "login_cases.json"
LOGIN_CASES = json.loads(DATA_FILE.read_text(encoding="utf-8"))


@pytest.mark.parametrize("case", LOGIN_CASES)
def test_login(case):
    resp = send_request(
        "POST",
        f"{BASE_URL}/api/v1/login",
        json={
            "username": case["username"],
            "password": case["password"],
        },
    )
    body = resp.json()

    assert resp.status_code == case["expected_status"]
    assert body["code"] == case["expected_code"]
    assert body["message"] == case["expected_message"]
```

### 你要理解

- Day 5 的测试文件开始更像“业务测试”
- 公共细节已经在慢慢下沉

---

## 10. 讲义模块七：迁移商品链路测试

新建：

```text
testcases/test_product_flow.py
```

内容：

```python
from common.assert_util import assert_success_response
from common.request_handler import send_request
from config.settings import BASE_URL


def test_product_create_query_delete(auth_headers):
    create_resp = send_request(
        "POST",
        f"{BASE_URL}/api/v1/products",
        headers=auth_headers,
        json={
            "name": "Desk Lamp",
            "category": "lighting",
            "price": 199,
        },
    )
    create_body = create_resp.json()

    assert_success_response(create_resp, create_body, status_code=201)
    product_id = create_body["data"]["id"]

    detail_resp = send_request("GET", f"{BASE_URL}/api/v1/products/{product_id}")
    detail_body = detail_resp.json()

    assert_success_response(detail_resp, detail_body)
    assert detail_body["data"]["id"] == product_id
    assert detail_body["data"]["name"] == "Desk Lamp"

    delete_resp = send_request(
        "DELETE",
        f"{BASE_URL}/api/v1/products/{product_id}",
        headers=auth_headers,
    )
    delete_body = delete_resp.json()

    assert_success_response(delete_resp, delete_body)
    assert delete_body["data"]["deleted"] is True
```

### 你要理解

- 这是 Day 5 的核心迁移成果
- 测试文件已经明显比 Day 3 更整洁了

---

## 11. 配套练习

### 练习 1

自己建出完整目录结构。

### 练习 2

自己把 `BASE_URL`、账号密码挪到 `config/settings.py`。

### 练习 3

自己写 `common/request_handler.py`。

### 练习 4

自己写 `common/assert_util.py`。

### 练习 5

把 Day 4 的登录测试迁移到 `testcases/`。

### 练习 6

把 Day 4 的商品链路测试迁移到 `testcases/`。

---

## 12. 课后作业

### 作业 1

给 `logger.py` 建一个最小版本，并在请求发送前后打印日志。

### 作业 2

给 `assert_util.py` 再加一个断言方法，用于失败响应，例如：

- 校验状态码
- 校验业务码
- 校验错误消息

### 作业 3

尝试把 `pytest.ini` 的 `testpaths` 配成 `testcases`，并确认 `pytest` 还能正常找到测试。

---

## 13. 今日建议节奏

建议按下面顺序来：

1. 20 分钟建目录
2. 20 分钟迁配置
3. 30 分钟写公共请求
4. 20 分钟写公共断言
5. 40 分钟迁移登录测试
6. 40 分钟迁移商品链路测试

---

## 14. 今日结束前的检查

你至少要确认下面这些事已经完成：

- 你已经有分层目录
- 你已经有 `common/request_handler.py`
- 你已经有 `config/settings.py`
- 你已经把至少 2 个测试文件迁移到了 `testcases/`
- 你已经能在新结构下继续跑 `pytest`

---

## 15. 如果你今天只想抓最核心的东西

那就只盯这 3 件事：

1. 配置集中管理
2. 请求统一入口
3. 测试文件只保留业务逻辑

只要这 3 件事做出来，Day 5 就已经达到重点。
